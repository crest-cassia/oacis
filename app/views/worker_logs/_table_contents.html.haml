- @logs.each do |log|
  - klass = log.level >= 3 ? :danger : (log.level==2 ? :warning : nil)
  %tr{id: dom_id(log), class: [dom_class(log), klass]}
    %td= log.worker
    %td= log.created_at
    %td= WorkerLog::SEVERITY[log.level]
    %td= log.message